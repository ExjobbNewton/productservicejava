version: '3.4'

services:
  productservicejava:
    image: productservicejava
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
  db:
    image: testproductservicedb
    build:
      context: .
      dockerfile: DockerfileDb

